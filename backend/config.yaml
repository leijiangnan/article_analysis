database:
  host: localhost
  port: 3306
  username: root
  password: password
  database: article_analysis
  driver: sqlite # 使用SQLite作为替代方案

server:
  port: 8080
  mode: debug

openai:
  api_key: your_openai_api_key_here
  api_base: https://api.moonshot.cn/v1  # Moonshot API基础URL，可自定义
  model: kimi-k2-0905-preview

log:
  level: info